# SUGGEST（ルート教材 D01.md から D15.md 向け）

この `SUGGEST.md` は **ルートフォルダの教材（`./D01.md` から `./D15.md`）** を読んだときの、
「つまずきやすいポイント」と「説明が明らかに不足している点」のメモです。
`previous_lesson/` 側の同名ファイルとは別物として扱います。

## 1) つまずきやすいポイント（先に注意しておくと良い）


### `./D04.md`（コールバック地獄）
- アロー関数＋ネスト＋閉じカッコの連続で、カッコ対応が追えずに折れやすい（内容ではなく形で詰む）。
- 「タイマーのコールバック」と「一般のコールバックパターン」が同じ“コールバック”として混ざりやすい。

### `./D05.md`（Promiseの概念）
- 「変数に入っているのはデータではなくPromise（チケット）」が直感に反して混乱ポイントになりやすい。
- `resolve/reject` の主語が「自分」ではなく「裏（Web API等）」という感覚が馴染むまで時間がかかる。

### `./D06.md`（then/catch とチェーン）
- `return` を忘れるとチェーンが切れる、が最大の落とし穴（“書き方のルール”として暗記になりがち）。
- 「`then` は次の `then` に値を渡す」「エラーは最後の `catch` に落ちる」など、伝搬の理解が必要。
- `new Promise((resolve, reject) => { ... })` は“中級の入口”で、いきなり難易度が跳ねやすい。

### `./D07.md`（async/await）
- `await` が使える場所（`async function` の中だけ）が初見だとよく躓く。
- 「async関数は必ずPromiseを返す」ため、呼び出し側での扱い（`main().catch(...)` 等）が意識しづらい。
- 「止まっているように見えてUIは止まらない」理由（イベントループとの接続）が腹落ちしにくい。

### `./D08.md`（try/catch/finally）
- `try/catch` が「例外」だけでなく「Promiseの失敗（reject）」にも効く点を混同しやすい。
- `finally` は便利だが、“いつ使うか”が分からないと記法だけ覚えて空回りしやすい。

### `./D09.md`（Promise.all）
- 「並列」という言葉で“スレッドで同時”と誤解しやすい（実態はI/O待ちを重ねる並行）。
- 分割代入 `const [user, friends] = ...` が初見だと読みづらい。
- 「1つでも失敗で全体失敗」が直感に反して事故りやすい。

### `./D10.md`（fetch GET）
- `await` が2回必要（`fetch` と `response.json()`）は理解できても、なぜ2回“非同期”なのかが残りやすい。
- API通信は環境要因（通信不可、CORS、URL間違い）で失敗し、学習者が自責しやすい。

### `./D11.md`（取得データをUIへ反映 / ローディング）
- 「HTMLの枠を用意→JSで要素取得→イベント→更新」の一連が、ファイル分割なしだと見通しが立ちにくい。
- `style.display` の切り替えは“魔法の呪文”に見えやすく、状態管理（表示/非表示）として整理が必要。

### `./D12.md`（fetch POST）
- `fetch(url, { method, headers, body })` の“設定オブジェクト”が長く、1要素でも欠けると動かないため挫折しやすい。
- `JSON.stringify` の必然性（通信はテキスト）が分かっていないと「なんで文字列に？」が再燃しやすい。

### `./D13.md`（response.ok / throw / AbortController）
- `fetch` は404でも `catch` に行かない（=ネットワーク自体は成功）という仕様が強烈な罠。
- `throw new Error(...)` で“自分でエラーにする”発想は、初心者にとって一段抽象度が上がる。
- `AbortController` は「いつ作って、どの通信を止めるのか」設計が必要で、コード例だけだと転用が難しい。

### `./D14.md`〜`./D15.md`（Web Worker）
- Workerは別ファイル必須・DOM不可・メッセージ連携必須で、ここまでのDOM学習とやり方が大きく変わって混乱しやすい。
- `new Worker('worker.js')` の“パス”と“動作条件（サーバー配信）”を知らないと、初手で動かず原因不明になりやすい。
- サンプルの「重い処理」はPC性能差で待ち時間が変わり、学習者が“自分の環境が悪い？”と不安になりやすい。

## 2) 本来説明すべきなのに「明らかに抜けている」ポイント

### 動かすための前提（最重要）
- **このリポジトリ（ルート）には、D10以降のサンプルをそのまま貼って動かすための `index.html` / `main.js` 等の雛形が無い。**
  「どこに書くか」「どのファイルを作るか」「どう開くか（ブラウザで開く/ローカルサーバーで開く）」の手順が必要。
- **Web Worker は `file://` 直開きでは動かないことが多い（同一オリジン制約）。**
  `http://localhost` で配信して動かす前提（例：VS Code Live Server / `python -m http.server` 等）の説明が必要。

### イベントループの“次の一段”
- Day3は「タスクキュー」までだが、Promiseに入ると **マイクロタスク（Microtask queue）** が絡み、
  `setTimeout(..., 0)` と `Promise.then(...)` の実行順が直感とズレやすい。ここを説明しないと、後半でログ順に混乱が起きやすい。

### fetchの失敗パターン整理
- Day13で `response.ok` は説明されるが、学習者が実際に詰むのはDay10〜12で起きるため、最低限以下の早期整理があると事故が減る：
  - **HTTPエラー（404/500）**：通信は成功だが `ok` がfalse（自分で分岐する）。
  - **ネットワークエラー**：`fetch` 自体が例外（`catch` に入る）。
  - **JSONとして読めない**：`response.json()` が例外になりうる（APIがHTMLを返す等）。

### async関数の“戻り値”と未処理エラー
- `async function` は常にPromiseを返すため、呼び出し側で `await` しない/`catch` しないと、**Unhandled Promise Rejection** が起きうる。
  「どこでawaitするか」「イベントハンドラ内のasyncの扱い」など、最低限のルール整理が必要。

### Worker運用の現実的な型
- `terminate()` は強制終了だが、現実では「キャンセル要求メッセージ→Worker側で中断」が必要な場面が多い。
  また、データコピーコスト（構造化複製）に触れてはいるが、「大きいデータは遅い」→「進捗を小さく刻む」等の実務的な繋げ方があると良い。
