# 📝 教材レビュー：改善提案（SUGGEST.md）

本ドキュメントは、JS5ヶ月目の女子大学生（イチカ丼）向け教材「非同期処理・集中講座（全15日）」に対するレビューです。

---

## 🔴 重要度：高（詰まりそうなポイント）

### 1. D06：`new Promise`の`resolve/reject`引数の意味と使い方が難解

**問題点**：
- D06.md の「6.4 チケットを自分で作ってみよう（new Promise）」セクションで、`new Promise((resolve, reject) => {...})` という構文が登場しますが、`resolve` と `reject` がどこから来るのか（引数として渡されること）の説明が初心者には分かりづらい可能性があります。
- 「resolve は関数」「reject も関数」であることは説明されていますが、**なぜ引数として受け取れるのか**（Promiseコンストラクタが内部で渡してくれる）の仕組みが省略されています。

**改善案**：
- 「Promiseを作る時、JavaScriptが自動で『成功ボタン(resolve)』と『失敗ボタン(reject)』を渡してくれる」というメタファーを追加する。
- 「この `resolve` や `reject` という名前は自由に変えられる（慣例的にこの名前を使う）」ことを明記する。

---

### 2. D07：`async function`が常にPromiseを返すことの説明不足

**問題点**：
- D07.md では「asyncをつけた瞬間、その関数は自動的にPromiseを返すようになります」と説明されていますが、**これが何を意味するか**の実践的な影響がやや薄い。
- 特に、「asyncでない普通の関数からasync関数を呼び出した場合の挙動」が説明されていない。

**改善案**：
- 以下のようなコード例を追加：
```javascript
// 普通の関数からasync関数を呼ぶと...
function normalFunction() {
    const result = asyncFunction(); // ← これはPromise！数値じゃない！
    console.log(result); // Promise {<pending>}
}
```
- 「だから呼び出し側でも `await` するか `.then()` が必要」という流れを強調する。

---

### 3. D08：「try...catch 再訪」だが、初学者にとっては初見かもしれない

**問題点**：
- D08.md のタイトルは「try...catch 再訪」となっていますが、PREVIOUS.md によれば「try-catch-finally を独立した文法要項として学習した」とあるものの、教材内にその詳細な内容がない。
- `throw new Error()` の構文がD13で突然登場し、D08では `throw` の説明がない。

**改善案**：
- D08の冒頭に「try-catch-finallyの復習ボックス」を設け、基本構文を図解付きでおさらいする。
- あるいは、PREVIOUS.mdに記載されている「try-catch-finally教材」へのリンクを追加する。

---

### 4. D13：`response.ok`と`throw`の組み合わせが唐突

**問題点**：
- D13.md で `throw new Error()` を使って手動でエラーを発生させる技法が登場しますが、**`throw`は初めて登場する**（D08では使っていない）。
- 「13.2 復習：`throw` と `Error` を思い出そう！」というセクションがあるものの、教材内でこれまで `throw` を使ったことがないため「思い出す」対象がない。

**改善案**：
- このセクションを「紹介」に変更し、`throw` が初登場であることを明確にする。
- または、D08で事前に `throw` を教えておく（エラーを自分で発生させるケースとして）。

---

### 5. D10：`response.json()`がなぜ`await`必要かの説明が後半すぎる

**問題点**：
- D10.md では「10.5 なぜ `await` が2回あるの？」で詳しく解説されていますが、コード例（10.4）が先に来るため、初学者は**コードを見た瞬間に混乱する可能性**がある。

**改善案**：
- コード例の前に「注目：`await` が2回あるよ！ 理由は後で説明するから、まずは『おまじない』として覚えてね」と前置きを入れる。
- または、10.4と10.5の順番を入れ替えて、概念説明→コードという流れにする。

---

## 🟡 重要度：中（説明が欲しいポイント）

### 6. アロー関数の復習が十分でない

**問題点**：
- D06.md で「Step 4：1行なら省略形も使える」としてアロー関数の省略形が紹介されていますが、**引数が複数の場合**や**戻り値がオブジェクトの場合**の書き方が説明されていない。

**改善案**：
```javascript
// 引数が2つ以上なら () 必須
(a, b) => a + b

// オブジェクトを返すなら () で囲む
() => ({ name: 'イチカ' })
```
- このパターンは特にPromiseの `.then()` チェーンで登場するため、補足として追加すると良い。

---

### 7. D09：分割代入（Destructuring）の説明が唐突

**問題点**：
- D09.md で `const [user, friends] = await Promise.all([...])` という分割代入が登場しますが、初学者には馴染みがない構文。
- 説明はあるものの、「配列の分割代入」しか触れられておらず、「オブジェクトの分割代入」との違いが分からなくなる可能性。

**改善案**：
- 「今回使うのは『配列の分割代入』だけ。オブジェクトの分割代入（`const { name } = user`）はまた別の話だから、今は気にしなくてOK！」と明記する。

---

### 8. D11：`style.display`の`block`と`none`以外の値の説明がない

**問題点**：
- D11.md で `style.display = 'block'` / `'none'` を使っていますが、CSSの`display`プロパティには `inline`、`flex`、`grid` など他の値もあることが触れられていない。
- 初学者が他の場所で `display` を使う際に困る可能性。

**改善案**：
- 「今回は `block`（表示）と `none`（非表示）だけ使うけど、CSSにはもっと種類があるよ。興味があったらMDNで調べてみてね！」と一言添える。

---

### 9. D12：`JSON.stringify` と `JSON.parse` の対応関係

**問題点**：
- D12.md に「おさらい：JSON変換の双子」というセクションがあり良い構成だが、`response.json()` が内部的に `JSON.parse` を使っていることの説明がさらっとしすぎている。

**改善案**：
- 「つまり、`response.json()` = `JSON.parse(response.text())` のショートカット」という図式を明確に示す。

---

### 10. D14-D15：Web Workerの実用シーンが不明確

**問題点**：
- Web Worker の概念は説明されているが、**実際のWebアプリでどんな時に使うのか**の具体例が曖昧。
- 「画像処理シミュレーション」という例はあるが、実際の画像データを扱っているわけではない。

**改善案**：
- 実用例として以下を追加：
  - マークダウンの構文解析
  - 大量データ（10万件）のフィルタリング
  - ゲームのAI計算
  - 暗号化/復号化処理

---

## 🟢 重要度：低（細かい改善提案）

### 11. D00：ロードマップSVGの画像パスが相対パスだが存在確認が必要

**確認事項**：
- `![講座ロードマップ](./zukai/D00_roadmap.svg)` のパスが正しいか確認（ファイルが存在するか）。

---

### 12. D00-D15：「💬「****************************」」というプレースホルダー

**問題点**：
- 多くの日で「セリフが未定」のプレースホルダーが残っている。
- ライブ配信で埋める予定とあるが、教材としては未完成感がある。

**改善案**：
- セリフを埋めるか、または「配信待ち」であることを目立つ形で表示する（例：背景色を変える）。

---

### 13. README.md と D00.md の「15日」vs「16日」の整合性

**問題点**：
- README.md では「全15日間」「D01.md から D16.md」と記載されているが、D16.md は「エピローグ」であり実質的な教材ではない。
- 日数の数え方が混乱する可能性。

**改善案**：
- 「教材は D01～D15（＋エピローグ）」と明記する。

---

### 14. D16.md（エピローグ）が存在しない

**問題点**：
- D15.md の最後に `<a href="D16.md">エピローグへ</a>` というリンクがあるが、D16.md ファイルが存在しない。

**改善案**：
- D16.md を作成するか、リンク先を変更する（例：README.mdに戻るなど）。

---

### 15. 画像ファイルの存在確認

**確認事項**：
- 各 `./sasie/` や `./zukai/` フォルダ内の画像ファイルが全て存在するか確認が必要。
- 特に `./sasie/D05_ticket_vs_coffee.png` など、コード中で参照されているファイル。

---

## 📚 その他の所見

### 良い点（維持・発展させるべき）

1. **メタファーの一貫性**：「ワンオペシェフ」「予約チケット」「キッチンタイマー」など、一貫したメタファーが非常に分かりやすい。
2. **キャラクターの活用**：「約束チケット姉さん」「りんりん」などのキャラクターが親しみやすさを演出。
3. **Mermaid図の活用**：シーケンス図やフローチャートで視覚的理解を促進。
4. **復習クイズ**：D03やD06にあるクイズは理解度確認に有効。
5. **「初心者さんの、心の旅」セクション**：学習者の内面的な理解過程を代弁しており共感しやすい。
6. **ダミー関数の提供**：`<details>` タグで折りたたまれたコピペ用コードは実験を容易にする。

### 将来の Java/バックエンド学習への布石

- D01 で「マルチスレッド」「C++/C#/Java」への言及があり、将来の学習への橋渡しになっている点は良い。
- 可能であれば、D13かD15の最後に「このあとJavaを学ぶと、マルチスレッドを自分で制御できるようになるよ」といったメッセージを追加すると動機付けになる。

---

## まとめ：優先対応リスト

| 優先度 | 対象 | 問題 |
|:---:|:---|:---|
| 🔴 | D13 | `throw` が初登場なのに「復習」となっている |
| 🔴 | D06 | `new Promise` の `resolve/reject` の仕組みが難解 |
| 🔴 | D07 | `async` 関数の戻り値がPromiseである影響の説明不足 |
| 🟡 | D09 | 分割代入の唐突な登場 |
| 🟡 | D10 | `await` 2回の説明がコード例より後に来る |
| 🟢 | D15 | D16.md へのリンクが壊れている |
| 🟢 | 全体 | プレースホルダーセリフの未記入 |
