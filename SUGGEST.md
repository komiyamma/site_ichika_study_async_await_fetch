# 👩‍🏫 文系・初学者向けカリキュラムの分析と提案

JavaScript学習4ヶ月目の文系学生（初学者）が、`previous_lesson`（運動トラッカー）から本カリキュラム（非同期処理・Fetch）に進むにあたり、つまずきやすいポイントと、補足が必要な点をまとめました。

## ⚠️ つまずきやすいポイント (Stumbling Blocks)



### 4. **Promise の「状態」という概念** (D05.md, D06.md)
   - **現象**: `Pending` `Fulfilled` `Rejected` という状態遷移は、目に見えないため非常に抽象的です。「チケット」というメタファーは秀逸ですが、「いつ商品に変わるのか？」「変えるのは誰なのか？」という主体が曖昧だと混乱します。
   - **箇所**: `D05.md` 全般。


---

## 🧩 明らかに不足している・補足すべき情報の提案 (Missing Information)

### 1. **`previous_lesson` との接続・応用**
   - **現状**: `previous_lesson` では `localStorage` を使っていましたが、今回のカリキュラムでは `fetch` で外部データを取ってきます。
   - **不足**: 「じゃあ、私が作った運動トラッカーをサーバーに保存するにはどうすればいいの？」という疑問に対するブリッジがありません。`GET` はやるものの、`POST` (D12予定) の実用的なイメージや、`localStorage` との使い分け（揮発性など）についての言及が薄いです。

### 2. **DOM操作との統合の実践例**
   - **現状**: `console.log` での確認が多くなっています。
   - **不足**: 生徒は「運動トラッカー」でDOM操作（`document.createElement` や `innerHTML`）を学んできたばかりです。非同期で取得したデータを使って、実際に画面（リストやカード）を作る具体的なステップが、特に `D11.md` 以降で手厚く必要です。「コンソールに出たからOK」ではなく、「画面に出して初めてOK」という感覚を維持させるべきです。

### 3. **「なぜ」JSONなのか？**
   - **現状**: `response.json()` が当たり前のように登場します。
   - **不足**: 初心者は「データ＝文字か数字」だと思っています。「なぜオブジェクトを文字（文字列）にして送るのか？」「なぜそのまま送れないのか？」という、JSONというフォーマットが存在する**根本的な理由（テキストベースの通信）**への軽いフォローがあると、腹落ちしやすくなります。

### 4. **DevTools (Networkタブ) の活用**
   - **現状**: コンソールログのみでのデバッグが中心です。
   - **不足**: 通信（Fetch）を学ぶなら、ブラウザの「Networkタブ」で本当にリクエストが飛んでいるか、何が返ってきているかを見る方法を教えるべきです。これは「裏側の世界」を可視化する唯一の手段であり、学習者の不安を解消する最強のツールです。
