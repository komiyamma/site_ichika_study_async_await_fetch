# 👩‍🏫 文系・初学者向けカリキュラムの分析と提案 (Round 2)

全体的な流れは非常にスムーズで、比喩（メタファー）も統一されており、分かりやすくなっています。
しかし、初学者が **「コードを書くとき」** に間違いやすい、あるいは **「おまじない」として流してしまいがち** な点が2点見つかりました。

## 🧩 不足している・補足すべき情報の提案

### 1. **`.then` チェーンにおける `return` の重要性** (D06.md)
   - **現象**: 6.3節のコード例で、当たり前のように `return process(data);` と書かれています。
   - **課題**: 初心者はこの `return` を忘れがちです。「次の関数を呼べばいい」と思って `return` を抜かすと、チェーンが切れて（次の `then` が待ってくれなくて）バグになります。
   - **提案**: 「なぜ `return` するのか？」の解説、あるいは「バトン（データ）を次の人に渡すために必要！」という注意書きを追加すべきです。

### 2. **`throw` という唐突なアクション** (D13.md)
   - **現象**: 13.2節で、404エラーを処理するために `throw new Error(...)` が登場します。
   - **課題**: 今までふわっとした比喩で進んできたのに、ここだけ急にプログラマっぽい `throw`（投げる）という単語が出てきます。「何を投げるの？」「投げたらどうなるの？」が直感的に繋がりません。
   - **提案**: `throw` ＝ **「緊急停止ボタンを押す」** または **「審判にレッドカードを提示する」** ようなアクションであり、**「それをすると強制的に `catch`（控え室）に送られる」** という機序を簡単なメタファーで補足すべきです。
